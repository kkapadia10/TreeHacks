"use strict";
function disableDragging(window) {
    window.addEventListener('dragstart', e => {
        preventDragListener(e, window);
    }, true);
}
const preventDragListener = (e, window) => {
    if (e.target != null
        && ((e.target instanceof window.HTMLElement && e.target.draggable === false)
            || e.target instanceof window.SVGElement)) {
        e.preventDefault();
        e.stopPropagation();
    }
};
let installed = false;
if (!installed && typeof window !== 'undefined') {
    disableDragging(window);
    installed = true;
}
