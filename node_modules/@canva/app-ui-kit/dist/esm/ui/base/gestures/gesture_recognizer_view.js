import { Preconditions } from '../../../base/preconditions';
import * as React from 'react';
import { GestureRecognizer } from './gesture_recognizer';
export class GestureRecognizerView extends React.PureComponent {
    constructor() {
        super(...arguments);
        this.inner = new GestureRecognizer(this.props.recognizers, this.props.raf, this.props.caf);
        this.gestureRef = (ele) => {
            var _a, _b;
            (_b = (_a = this.inner).setRef) === null || _b === void 0 ? void 0 : _b.call(_a, ele);
        };
    }
    render() {
        return this.props.children({ gestureRef: this.gestureRef });
    }
    componentDidUpdate(prevProps) {
        Preconditions.checkArgument(prevProps.recognizers === this.props.recognizers, 'Recognizers can not change after initial render.');
    }
}
