import { Fragment as _Fragment, jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import * as React from 'react';
import styles from './conjoined_buttons.css';
export function ConjoinedButtons({ children }) {
    const validChildren = React.Children.toArray(children).filter(React.isValidElement);
    if (validChildren.length < 2) {
        return _jsx(_Fragment, { children: validChildren });
    }
    return (_jsx("div", { className: styles.conjoinedButtons, children: validChildren.map((child, i) => React.cloneElement(child, {
            className: classNames(child.props.className, getConjoinedClass(i, validChildren.length)),
        })) }));
}
const getConjoinedClass = (i, n) => {
    if (i === 0) {
        return styles.first;
    }
    if (i === n - 1) {
        return styles.last;
    }
    return styles.rest;
};
