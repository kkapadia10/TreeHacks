import { makeObservable } from '../../../../base/make_observable/make_observable';
import { __decorate } from "tslib";
import { action, observable } from 'mobx';
export class SubmitStore {
  static _makeObservable(instance) {
    makeObservable(instance, {
      shouldDisableSubmit: observable.ref,
      enable: action
    });
  }
  constructor() {
    SubmitStore._makeObservable(this);
    this.shouldDisableSubmit = true;
  }
  enable() {
    this.shouldDisableSubmit = false;
  }
}