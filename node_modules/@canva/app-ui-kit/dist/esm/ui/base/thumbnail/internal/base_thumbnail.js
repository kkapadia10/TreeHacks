import { jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import * as React from 'react';
import { AspectRatio } from '../../aspect_ratio/aspect_ratio';
import styles from './base_thumbnail.css';
export const BaseThumbnail = ({ children, className, background = 'none', border = false, borderRadius = 'none', aspectRatio, width, height, padding, ariaLabel, }) => {
    const thumbnail = (_jsx("div", { className: classNames(styles.baseThumbnail, getWrapperClasses({ background, border, borderRadius, className, padding })), style: aspectRatio == null ? { width, height } : {}, "aria-label": ariaLabel, children: children }));
    if (aspectRatio != null) {
        return (_jsx(AspectRatio, { ratio: aspectRatio, width: width, height: height, children: thumbnail }));
    }
    return thumbnail;
};
const getWrapperClasses = ({ background, border, borderRadius, className, padding, }) => {
    const backgroundClasses = {
        [styles.secondaryBackground]: background === 'secondary',
        [styles.contrastOnLight]: background === 'contrastOnLight',
        [styles.contrastOnDark]: background === 'contrastOnDark',
    };
    const borderClasses = {
        [styles.border]: border,
        [styles.smallBorderRadius]: borderRadius === 'small',
        [styles.mediumBorderRadius]: borderRadius === 'medium',
    };
    const paddingClasses = {
        [styles.paddingOneUnit]: padding === '1u',
        [styles.paddingTwoUnit]: padding === '2u',
    };
    return classNames(styles.baseThumbnail, backgroundClasses, borderClasses, paddingClasses, className);
};
