import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { BaseThumbnail } from '../internal/base_thumbnail';
import styles from './icon_thumbnail.css';
export function IconThumbnail({ tone = 'secondary', Icon, size, background = 'secondary', border, padding, borderRadius, aspectRatio, width, height, }) {
    return (_jsx(BaseThumbnail, { background: background, border: border, borderRadius: borderRadius, className: styles.iconThumbnail, aspectRatio: aspectRatio, padding: padding, width: width, height: height, children: _jsx(Icon, { tone: tone, size: size || 'medium' }) }));
}
