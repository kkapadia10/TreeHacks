import classNames from 'classnames';
import * as React from 'react';
import { commonPropsToDOMProps } from '../../box/types';
import styles, { getStyle } from './container.css';
export const Container = React.forwardRef(({ children, width, tagName = 'div', ...rest }, ref) => {
    return React.createElement(tagName, {
        className: classNames(styles.container, getStyle(width)),
        children,
        ref,
        ...commonPropsToDOMProps(rest),
    });
});
Container.displayName = 'Container';
