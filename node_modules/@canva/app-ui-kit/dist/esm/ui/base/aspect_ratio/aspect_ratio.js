import { jsx as _jsx } from "react/jsx-runtime";
import { Preconditions } from '../../../base/preconditions';
import * as React from 'react';
import styles from './aspect_ratio.css';
export const AspectRatio = ({ ratio, children, width, height }) => {
    Preconditions.checkArgument(ratio > 0, 'Ratio must be greater than 0');
    if (width == null && height == null) {
        const containerStyles = { paddingTop: `${(1 / ratio) * 100}%` };
        return (_jsx("div", { className: styles.container, style: containerStyles, children: _jsx("div", { className: styles.content, children: children }) }));
    }
    if (width != null) {
        return _jsx("div", { style: { width, height: width / ratio }, children: children });
    }
    return _jsx("div", { style: { height, width: height * ratio }, children: children });
};
