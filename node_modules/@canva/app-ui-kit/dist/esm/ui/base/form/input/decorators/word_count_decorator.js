import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { useInputValue } from '../base_input';
import { CharsRemaining } from '../chars_remaining';
export const WordCountDecorator = React.memo(function WordCountDecorator({ max, }) {
    var _a;
    const value = ((_a = useInputValue()) !== null && _a !== void 0 ? _a : '').trim();
    const count = value.length ? value.split(/[\s\n]+/).length : 0;
    return _jsx(CharsRemaining, { count: count, max: max });
});
