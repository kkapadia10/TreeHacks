import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import * as ReactDOM from 'react-dom';
import styles from './screen_reader_content.css';
function useWindow() {
    const [globalWindow, setGlobalWindow] = React.useState();
    React.useEffect(() => {
        setGlobalWindow(window);
    }, []);
    return globalWindow;
}
export const ScreenReaderContent = ({ children, ariaLive, ariaAtomic, ariaRelevant, id, tagName: TagName = 'div', role, }) => {
    const window = useWindow();
    const content = (_jsx(TagName, { id: id, role: role, "aria-live": ariaLive, "aria-atomic": ariaAtomic, "aria-relevant": ariaRelevant, 
        className: styles.screenReaderContent, children: children }));
    if (id != null) {
        if (window != null) {
            return ReactDOM.createPortal(content, window.document.body);
        }
        return null;
    }
    return content;
};
