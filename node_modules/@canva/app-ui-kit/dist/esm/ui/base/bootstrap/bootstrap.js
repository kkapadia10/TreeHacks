import { loadFromWindow } from '../../../base/bootstrap/bootstrap';
import { uiBootstrapKey } from './key';
import { UiBootstrap } from './ui_bootstrap_proto';
export const bootstrap = loadFromWindow(uiBootstrapKey, UiBootstrap.deserialize, {
    applyOverrides: true,
});
