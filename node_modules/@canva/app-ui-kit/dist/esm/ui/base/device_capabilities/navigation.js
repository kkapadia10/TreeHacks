import { smallBreakpoint } from '../metrics/metrics';
import { getScreenSize } from './screen';
export function preferSameTab() {
    const { width: screenWidth, height: screenHeight } = getScreenSize();
    return (screenWidth > 0 && screenHeight > 0 && (screenWidth < smallBreakpoint || screenHeight < smallBreakpoint));
}
