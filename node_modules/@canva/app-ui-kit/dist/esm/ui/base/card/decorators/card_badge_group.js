import { jsx as _jsx } from "react/jsx-runtime";
import { observer } from 'mobx-react';
import * as React from 'react';
import { pointerInputState } from '../../a11y/pointer_input_state/pointer_input_state';
import { BadgeGroup } from '../../badge/badge_container/badge_container';
import { useCardConfiguration } from '../internal/card_context';
export const CardBadgeGroup = observer(function CardBadgeGroup({ forceShow, ...props }) {
    var _a, _b;
    const { location } = props;
    const { badgeTransition } = useCardConfiguration();
    const locatedAtTop = location === 'top-start' || location === 'top-end';
    const transition = locatedAtTop ? (_a = props.transition) !== null && _a !== void 0 ? _a : badgeTransition : 'fade';
    const visibility = !pointerInputState.isMouseInput || forceShow ? 'always' : (_b = props.visibility) !== null && _b !== void 0 ? _b : 'on-hover';
    return _jsx(BadgeGroup, { ...props, transition: transition, visibility: visibility });
});
