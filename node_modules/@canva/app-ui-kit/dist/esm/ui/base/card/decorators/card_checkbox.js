import { jsx as _jsx } from "react/jsx-runtime";
import { observer } from 'mobx-react';
import * as React from 'react';
import { Badge } from '../../badge/badge';
import { SwapOnHover } from '../../badge/badge_container/badge_container';
import { Checkbox } from '../../form/checkbox/checkbox';
import { useCardConfiguration } from '../internal/card_context';
import { CardBadgeGroup } from './card_badge_group';
export const CardCheckbox = observer(function CardCheckbox(props) {
    const { onSelect, selected, status, label, forceShow } = props;
    const configuration = useCardConfiguration();
    const showCheckbox = selected || forceShow;
    const disabled = props.disabled || configuration.disabled;
    const checkbox = (_jsx(Checkbox, { size: "medium", onChange: onSelect, checked: selected, disabled: disabled, ariaLabel: label }));
    return status && !showCheckbox
        ? (_jsx(CardBadgeGroup, { location: "top-start", visibility: "always", children: _jsx(SwapOnHover, { onHover: checkbox, children: status && _jsx(Badge, { tone: "contrast", text: status }) }) }))
        : (_jsx(CardBadgeGroup, { location: "top-start", forceShow: showCheckbox, children: checkbox }));
});
