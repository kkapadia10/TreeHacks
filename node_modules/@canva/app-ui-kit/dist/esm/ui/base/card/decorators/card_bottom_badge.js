import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { Badge } from '../../badge/badge';
import { CardBadgeGroup } from './card_badge_group';
export function CardBottomBadge({ message, Icon, location = 'start', forceShow, badgeClassName, }) {
    return message || Icon
        ? (_jsx(CardBadgeGroup, { location: location === 'end' ? 'bottom-end' : 'bottom-start', forceShow: forceShow, children: _jsx(Badge, { tone: "contrast", icon: Icon, text: message, className: badgeClassName }) }))
        : null;
}
