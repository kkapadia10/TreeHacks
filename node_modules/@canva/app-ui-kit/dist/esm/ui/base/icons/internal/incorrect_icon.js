import { jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import * as React from 'react';
import styles from './incorrect_icon.css';
export const createIncorrectlySizedIcon = (MyIcon) => {
    const Component = (props) => {
        const { className, ...rest } = props;
        return (_jsx("span", { className: classNames(styles.container, className), children: _jsx(MyIcon, { ...rest, size: "small" }) }));
    };
    Component.sizes = { medium: MyIcon.sizes.small };
    return Component;
};
