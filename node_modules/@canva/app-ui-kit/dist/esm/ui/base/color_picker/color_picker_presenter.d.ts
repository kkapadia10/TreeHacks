import { HsvColor } from './hsv_color/hsv_color';
export type ColorStore = {
    color: string;
    alpha: number;
};
export declare class ColorStoreImpl implements ColorStore {
    color: string;
    alpha: number;
    constructor(color: string, alpha: number);
}
export declare class ColorPickerStore {
    private readonly colorStore;
    lastHsvColor: HsvColor | undefined;
    constructor(colorStore: ColorStore);
    get color(): HsvColor;
}
export declare class ColorPickerStores {
    lastHsvColor: HsvColor | undefined;
    _color: string;
    constructor(color: string);
    get color(): HsvColor;
}
export declare class ColorPickerPresenter {
    private readonly store;
    private readonly onChange;
    constructor(store: ColorPickerStore, onChange: (color: string, source: 'hex' | 'satVal' | 'hue' | 'alpha', alpha?: number) => void);
    onInputChange(hex: string): void;
    onSatValChange(color: HsvColor): void;
    onHueChange(hue: number): void;
    onAlphaChange(alpha: number): void;
}
