import * as React from 'react';
import { Component } from 'react';
import { HsvColor } from '../hsv_color/hsv_color';
import type { DraggableProps } from '../../draggable/draggable_view';
export type Props = {
    color: HsvColor
    onChange(color: HsvColor): void
    onChangeStart?(): void
    onChangeComplete?(): void
    Draggable: React.ComponentType<DraggableProps>
};
export declare class SatValPicker extends Component<Props> {
    private pickerRef?;
    private readonly captureRef;
    private changing;
    render(): import("react/jsx-runtime").JSX.Element;
    private moveKnob;
    private onChange;
    private readonly stopChanging;
    private readonly onKeyDown;
    private readonly onKeyUp;
    private readonly onMouseUp;
    private readonly onMouseDown;
    private readonly onDragMove;
    private readonly onDragEnd;
}
export declare function mouseToKnobCoords(pickerRect: DomRect, { clientX, clientY }: MouseCoords): KnobCoordsFromCorner;
type DomRect = {
    left: number;
    top: number;
    width: number;
    height: number;
};
type KnobCoordsFromCorner = {
    leftPercent: number;
    topPercent: number;
};
type MouseCoords = {
    clientX: number;
    clientY: number;
};
export {};
