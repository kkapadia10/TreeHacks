import * as React from 'react';
import type { TouchOrMouseEvent } from '../button/button';
import type { ColorPickerInputGroupProps } from './internal/internal_color_picker';
export type ColorPickerProps = ColorPickerInteractionProps & {
    color: string
    alpha?: number
    sizingMode?: 'fixed' | 'fill'
    isSheet?: boolean
    onChangeStart?(): void
    onChangeComplete?(): void
} & Pick<ColorPickerInputGroupProps, 'autoFocusHexInput'>;
type ColorPickerInteractionProps = {
    onChange(color: string, source: 'hex' | 'satVal' | 'hue' | 'alpha', alpha?: number): void
    onStartEyedropper?(event: TouchOrMouseEvent<any>): Promise<void>
    onDeleteColor?(): void
};
export declare class ColorPicker extends React.Component<ColorPickerProps> {
    private readonly colorStore;
    private readonly store;
    private presenter;
    constructor(props: ColorPickerProps);
    componentDidUpdate(previousProps: ColorPickerProps): void;
    render(): import("react/jsx-runtime").JSX.Element;
}
export {};
