import { jsx as _jsx } from "react/jsx-runtime";
import { CheckboxGroup as EaselCheckboxGroup } from '../../../../../../base/form/checkbox/checkbox_group';
/** 
 * An accessible list of checkboxes.
 */
export function CheckboxGroup(props) {
    return (_jsx(EaselCheckboxGroup, { defaultValue: props.defaultValue, disabled: props.disabled, id: props.id, onChange: props.onChange, options: props.options, value: props.value }));
}
