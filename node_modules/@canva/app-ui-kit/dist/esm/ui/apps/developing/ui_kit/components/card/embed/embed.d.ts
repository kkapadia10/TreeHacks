import type { InternalCardProps } from '../internal_card';
/** 
 * The props for the `EmbedCard` component.
 */
export type EmbedCardProps = InternalCardProps & {
    /** 
         * An accessible description of what happens when the card is clicked. If omitted, the `title` prop is used instead.
         * If there is no `title`, this prop must be set.
         * @example "Add embed to design"
         */
    ariaLabel?: string;
    /** 
         * The URL of an image to render as the card's thumbnail.
         */
    thumbnailUrl: string;
    /** 
         * A human readable title for the card.
         */
    title: string;
    /** 
         * A human readable description that appears beneath the title.
         */
    description?: string;
};
export declare const EmbedCard: ({ ariaLabel, title, description, thumbnailUrl, onDragStart, onClick, }: EmbedCardProps) => import("react/jsx-runtime").JSX.Element;
