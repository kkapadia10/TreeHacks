import { jsx as _jsx } from "react/jsx-runtime";
import { RadioGroup as EaselRadioGroup } from '../../../../../../base/form/radio_group/radio_group';
/** 
 * An accessible list of radio buttons.
 */
export function RadioGroup(props) {
    return (_jsx(EaselRadioGroup, { defaultValue: props.defaultValue, disabled: props.disabled, id: props.id, name: props.name, onChange: props.onChange, options: props.options, value: props.value }));
}
