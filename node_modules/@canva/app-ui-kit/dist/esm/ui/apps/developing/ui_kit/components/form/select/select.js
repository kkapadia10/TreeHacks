import { jsx as _jsx } from "react/jsx-runtime";
import { Select as EaselSelect } from '../../../../../../base/form/select/select';
/** 
 * A dropdown with a selectable list of options.
 */
export function Select(props) {
    return (_jsx(EaselSelect, { disabled: props.disabled, error: props.error, id: props.id, options: props.options, onChange: props.onChange, placeholder: props.placeholder, stretch: props.stretch, type: "default", value: props.value }));
}
