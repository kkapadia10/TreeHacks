import { jsx as _jsx } from "react/jsx-runtime";
import { CardDecorator, HorizontalCard } from '../../../../../../base/card/card';
import { formatDuration } from '../../../../../../base/date_time/duration/duration';
import { AudioCover } from './audio_cover/audio_cover';
export const AudioCard = ({ ariaLabel, audioPreviewUrl, durationInSeconds, title, thumbnailUrl, onDragStart, onClick, }) => {
    const description = formatDuration(durationInSeconds);
    return (_jsx(HorizontalCard, { hasHoverEffect: true, border: false, size: "small", buttonAriaLabel: ariaLabel, title: title, description: description, onClick: onClick, draggable: onDragStart !== undefined, onDragStart: onDragStart, children: _jsx(CardDecorator, { children: _jsx(AudioCover, { url: audioPreviewUrl, thumbnailUrl: thumbnailUrl }) }) }));
};
