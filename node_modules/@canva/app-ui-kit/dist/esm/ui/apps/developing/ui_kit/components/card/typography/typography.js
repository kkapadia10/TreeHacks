import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { Box } from '../../../../../../base/box/box';
import { BasicButton } from '../../../../../../base/button/button';
export const TypographyCard = ({ ariaLabel, onClick, onDragStart, children, }) => {
    const [isDragging, setIsDragging] = React.useState(false);
    return (_jsx(BasicButton, { ariaLabel: ariaLabel, onClick: onClick, draggable: onDragStart !== undefined, onDragStart: event => {
            if (onDragStart === undefined) {
                return;
            }
            setIsDragging(true);
            onDragStart(event);
        }, onDragEnd: () => {
            setIsDragging(false);
        }, noChildWrapper: true, children: _jsx(Box, { background: "neutralLow", padding: "2u", borderRadius: "standard", children: _jsx("span", { style: { opacity: isDragging ? 0 : 1 }, children: children }) }) }));
};
