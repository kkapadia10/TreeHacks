import { configure as mobxConfigure } from 'mobx';
import { uiBootstrapKey } from '../../../base/bootstrap/key';
import { Direction, Theme, UiBootstrap } from '../../../base/bootstrap/ui_bootstrap_proto';
initializeEaselBootstrap({
    enableAnimations: true,
    direction: Direction.LTR,
    disableFocusTraps: true,
    enableUserSelection: true,
    disableDialogBlur: false,
    exposeStringIds: false,
    enableHighColorContrast: false,
    autoplayVideos: undefined,
    theme: Theme.DARK,
    enableEaselUiRefresh: false,
});
mobxConfigure({
    enforceActions: 'observed',
    isolateGlobalState: true,
});
function initializeEaselBootstrap(bootstrap) {
    document.documentElement.dir = bootstrap.direction === Direction.RTL ? 'rtl' : 'ltr';
    window['bootstrap'] = {
        [uiBootstrapKey]: UiBootstrap.serialize({
            ...bootstrap,
            direction: bootstrap.direction,
        }),
    };
}
