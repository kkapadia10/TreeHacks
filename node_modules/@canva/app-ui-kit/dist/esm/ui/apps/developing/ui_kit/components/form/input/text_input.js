import { jsx as _jsx } from "react/jsx-runtime";
import { TextInput as EaselTextInput } from '../../../../../../base/form/input/text_input';
/** 
 * An input that accepts textual values.
 * */
export function TextInput(props) {
    return (_jsx(EaselTextInput, { defaultValue: props.defaultValue, disabled: props.disabled, error: props.error, id: props.id, type: props.type || 'text', maxLength: props.maxLength, name: props.name, onChange: props.onChange, placeholder: props.placeholder, value: props.value }));
}
