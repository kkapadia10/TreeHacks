export declare class WriteBuffer {
    private readonly bufferSize;
    private reservedSizes;
    private buffer;
    private pastBuffers;
    private bufferIndex;
    private byteSize;
    private openReservations;
    constructor(bufferSize?: number);
    createSizeReservation(): () => number;
    write(byte: number): void;
    writeFloat(float: number): void;
    writeBytes(bytes: Uint8Array | readonly number[]): void;
    flush(): Uint8Array;
}
