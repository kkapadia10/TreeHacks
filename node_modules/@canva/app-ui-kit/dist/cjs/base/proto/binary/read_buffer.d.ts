export declare class ReadBuffer {
    private currentChunk;
    private readonly chunkQueue;
    private cursor;
    private remaining;
    private bytesRead_;
    private reader;
    constructor(stream: ReadableStream<Uint8Array>);
    loadBytes(amount: number, force?: boolean): Promise<void>;
    get bytesRead(): number;
    read(): number;
    readFloat(): number;
}
