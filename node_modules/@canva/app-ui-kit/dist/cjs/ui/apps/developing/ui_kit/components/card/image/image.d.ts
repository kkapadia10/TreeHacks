import type { InternalCardProps } from '../internal_card';
/** 
 * The props for the `ImageCard` component.
 */
export type ImageCardProps = InternalCardProps & {
    /** 
         * An accessible description of what happens when the card is clicked.
         * @example "Add image into design"
         */
    ariaLabel: string;
    /** 
         * The URL of an image to render as the card's thumbnail.
         */
    thumbnailUrl: string;
    /** 
         * A human readable description of the image. This should not be set if `ariaLabel` describes the image.
         */
    alt?: string;
    /** 
         * The rounding of the card's corners.
         * @default "none"
         */
    borderRadius?: 'none' | 'standard';
    /** 
         * A fixed height for the card, regardless of its contents. If omitted, the card grows to fit the size of its contents.
         */
    thumbnailHeight?: number;
};
export declare const ImageCard: ({ alt, ariaLabel, thumbnailUrl, onDragStart, onClick, borderRadius, thumbnailHeight, }: ImageCardProps) => import("react/jsx-runtime").JSX.Element;
