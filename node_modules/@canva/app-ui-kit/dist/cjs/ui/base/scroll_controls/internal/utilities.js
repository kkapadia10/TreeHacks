"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.calculateScrollStatePosition = void 0;
const normalized_scroll_1 = require("../normalized_scroll");
const calculateScrollStatePosition = (scrollableEl, layout, isRtl) => {
    let containerSize, scrollSize, scrollPosition;
    if (layout === 'horizontal') {
        scrollSize = scrollableEl.scrollWidth;
        containerSize = scrollableEl.clientWidth;
        scrollPosition = isRtl
            ? scrollSize - containerSize - (0, normalized_scroll_1.getNormalizedScrollLeft)(scrollableEl, isRtl)
            : (0, normalized_scroll_1.getNormalizedScrollLeft)(scrollableEl, isRtl);
    }
    else {
        scrollSize = scrollableEl.scrollHeight;
        containerSize = scrollableEl.clientHeight;
        scrollPosition = scrollableEl.scrollTop;
    }
    scrollPosition = Math.min(Math.max(scrollPosition, 0), scrollSize - containerSize);
    return { scrollPosition, containerSize };
};
exports.calculateScrollStatePosition = calculateScrollStatePosition;
