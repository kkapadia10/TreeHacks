import * as React from 'react';
import type { Focusable } from '../../../focusable/focusable';
export type RadioItemProps<T> = {
    label: React.ReactNode;
    description?: string;
    value?: T;
    disabled?: boolean;
    checked?: boolean;
    className?: string;
    name?: string;
    ariaLabel?: string;
    focusRef?: React.RefObject<Focusable>;
    onChange?(value: T, event: React.ChangeEvent): void;
    onFocus?: React.FocusEventHandler;
    onBlur?: React.FocusEventHandler;
};
export declare function RadioItem<T>(
 { label, description, className, disabled, checked, ariaLabel, focusRef, name, value, onChange: _onChange, onFocus, onBlur, }: RadioItemProps<T>
): import("react/jsx-runtime").JSX.Element;
