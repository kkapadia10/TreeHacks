"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useViewportHeightDifference = void 0;
const React = require("react");
const calculation = 'calc(100lvh - 100svh)';
const supportsCss = () => window.CSS && window.CSS.supports(`top: ${calculation}`);
function subscribe(callback) {
    if (supportsCss()) {
        return () => { };
    }
    window.addEventListener('resize', callback);
    return () => {
        window.removeEventListener('resize', callback);
    };
}
function getSnapshot() {
    if (supportsCss()) {
        return calculation;
    }
    return `${window.innerHeight - document.documentElement.clientHeight}px`;
}
function useViewportHeightDifference() {
    return React.useSyncExternalStore(subscribe, getSnapshot);
}
exports.useViewportHeightDifference = useViewportHeightDifference;
