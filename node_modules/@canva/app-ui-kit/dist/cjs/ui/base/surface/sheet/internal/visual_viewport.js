"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useVisualViewport = void 0;
const React = require("react");
function useVisualViewport() {
    const [state, setState] = React.useState();
    React.useLayoutEffect(() => {
        if (!window.visualViewport) {
            return;
        }
        const onVisualViewportChange = () => {
            if (window.visualViewport) {
                setState({
                    height: window.visualViewport.height + 'px',
                });
            }
            else {
                setState(undefined);
            }
        };
        onVisualViewportChange();
        window.visualViewport.addEventListener('resize', onVisualViewportChange);
        window.visualViewport.addEventListener('scroll', onVisualViewportChange);
        return () => {
            var _a, _b;
            (_a = window.visualViewport) === null || _a === void 0 ? void 0 : _a.removeEventListener('resize', onVisualViewportChange);
            (_b = window.visualViewport) === null || _b === void 0 ? void 0 : _b.removeEventListener('scroll', onVisualViewportChange);
        };
    }, []);
    return state;
}
exports.useVisualViewport = useVisualViewport;
