"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useDrag = void 0;
const React = require("react");
const gesture_recognizer_1 = require('../../../gestures/gesture_recognizer');
function useDrag({ resizeControls }) {
    const [recognizer] = React.useState(new gesture_recognizer_1.GestureRecognizer([
        new gesture_recognizer_1.PanRecognizer({
            minDistance: 0,
            direction: 'vertical',
            onStart: resizeControls.start,
            onMove: event => resizeControls.update(-event.distanceY),
            onEnd: resizeControls.end,
        }),
    ]));
    return { dragRef: recognizer.setRef };
}
exports.useDrag = useDrag;
