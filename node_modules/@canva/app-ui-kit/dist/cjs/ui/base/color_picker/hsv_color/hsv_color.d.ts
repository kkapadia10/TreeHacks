import { RgbaColor, RgbColor } from '../../color/color';
export declare class HsvColor {
    readonly h: number;
    readonly s: number;
    readonly v: number;
    readonly alpha: number;
    constructor(h: number, s: number, v: number, alpha?: number);
    static fromHexString(hex: string): HsvColor;
    matchHexString(target: string): HsvColor;
    toRgb(): RgbColor;
    toRgba(): RgbaColor;
    equals(that: HsvColor): boolean;
}
