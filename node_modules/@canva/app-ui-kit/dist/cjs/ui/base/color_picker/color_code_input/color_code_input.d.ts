import * as React from 'react';
import type { TextInputProps } from '../../form/input/text_input';
type ColorCodeInputProps = {
    color: string
    alpha: number
    onChange(color: string): void
    onAlphaChange?(alpha: number): void
    withSwatch?: boolean
} & Pick<TextInputProps, 'autoFocus' | 'ariaLabel' | 'ariaLabelledBy' | 'ariaDescribedBy'>;
export declare class ColorCodeInput extends React.Component<ColorCodeInputProps> {
    private input;
    private inputValue;
    private recentlyBlurred;
    render(): import("react/jsx-runtime").JSX.Element;
    private renderInput;
    private renderAlphaInput;
    private readonly onFocus;
    private readonly onBlur;
    private onChange;
    private onAlphaChange;
}
export {};
